# Schema

> Placeholder. The full schema definition (node types, edge types, frontmatter fields, profile mechanism) lands with v0.1.0 — see the milestone in [ROADMAP.md](../ROADMAP.md) and the design in [PRD.md §7](../PRD.md).

When v0.1.0 ships, this document will contain:

- The 10 inherited Infinite Brain epistemic node types.
- The company entity node types (`product`, `persona`, `competitor`, `requirement`, etc.).
- The profile-conditional `medical-device` node types (`indication-for-use`, `regulatory-clearance`, `hazard`, etc.).
- The 10 edge types and when to use each.
- Required and conditional frontmatter fields.
- The validation rules enforced by `cb validate`.
